[package]
name = "sayall-hud"
version = "0.1.6"
edition = "2024"
license = "MIT"

[dependencies]
gtk = { package = "gtk4", version = "0.10" }
gtk4-layer-shell = "0.7"
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
